<!-- 搜索框 -->
<search></search>

<!-- 顶部分类 tab（横向可滚动） -->
<scroll-view class="course-tabbar" scroll-x="true" scroll-with-animation="true">
  <view wx:for="{{tabs}}" wx:key="type"
        class="tab-item {{currentTab === item.type ? 'active': ''}}"
        data-type="{{item.type}}"
        bindtap="switchTab">
    {{item.name}}
  </view>
</scroll-view>

<!-- 课程列表容器 -->
<view class="course-list-wrap">
  <!-- 如果列表为空显示提示 -->
  <view wx:if="{{courseList.length === 0}}" class="empty-tip">
    暂无课程，换个分类看看吧～
  </view>

  <!-- 课程卡片网格 -->
  <view class="course-grid">
    <block wx:for="{{courseList}}" wx:key="id">
      <view class="course-card" data-id="{{item.id}}" bindtap="openCourseDetail">
        <image class="cover" src="{{item.cover}}" mode="aspectFill" />
        <view class="card-body">
          <view class="title">{{item.title}}</view>
          <view class="meta">
            <view class="teacher">{{item.teacher}}</view>
            <view class="price">{{item.price}}</view>
          </view>
        </view>
      </view>
    </block>
  </view>
</view>
